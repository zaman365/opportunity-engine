# M0 · Foundation

**Complete.** Supersedes `opportunity-engine-build-kit/tasks/M0_FOUNDATION.md`; scope unchanged.

Configuration that fails closed and names the variable it refused; four roles with a
membership resolved from a verified identity; tenancy with `FORCE ROW LEVEL SECURITY` and no
runtime role that can bypass it; a budget ledger whose writes go only through SQL command
functions; and a contract the application is tested against rather than described by.

See [ADR-010](../adr/ADR-010-toolchain.md) through
[ADR-014](../adr/ADR-014-dispatch-index.md).
